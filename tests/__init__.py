"""TRACER test suite.

Declared as a package so intra-suite imports (``from tests.synthetic import
...``, ``tests.segmentation_sim``, ``tests._pipeline_runner``) resolve to this
directory and are not shadowed by any unrelated ``tests`` package that may be
installed in the environment's site-packages.
"""
